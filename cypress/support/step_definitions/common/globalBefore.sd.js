before(() => {
  // cy.log(
  //   "This is the beginning of tests"
  // );
});

beforeEach(() => {
  // cy.log(
  //   "This is the beginning of every test"
  // );
});

afterEach(() => {
  // cy.log(
  //   "This is the end of every test"
  // );
});

after(() => {
  // cy.log(
  //   "This is the end of tests"
  // );
});
